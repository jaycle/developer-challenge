export interface Branch {
  id: number;
  name: string;
  accountAddress: string;
}

export interface Book {
  id: number;
  title: string;
  isbn: string;
  author: string;
  address: string;
  tokenId: string;
  status: string;
}
